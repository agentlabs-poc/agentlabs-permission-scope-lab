# Retired interactive pages — archived source only

The user explicitly approved removing page 2 (Projects & Repositories guided
explorer) and page 3 (standalone enforcement trace) from the active site. Page 1,
the HRMS/Payroll explorer, remains available. The Projects handbook chapter is
not the retired simulation and also remains available.

These byte-for-byte snapshots preserve the local files immediately before
removal, including the preceding reconciliation notices:

- [Projects explorer HTML](projects-explorer.html.txt)
- [Projects explorer implementation and scenarios](src/projects-explorer.ts.txt)
- [Standalone enforcement trace](enforcement-trace.html.txt)

The `.txt` extension makes these historical source records, not runnable pages.
The retired URLs are no longer build entries or navigation destinations and
return 404 in the development/preview servers. Recovering a prototype means
restoring its archived source and entry point deliberately; it must not silently
become the canonical architecture again.

No commit or push is authorized. The user's gate remains frozen.
