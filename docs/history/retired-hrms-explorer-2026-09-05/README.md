# Retired HRMS guided explorer — archived source only

The user subsequently requested removal of the remaining guided explorer.
The handbook now occupies `/`; its existing `concept.html` entry and Concept,
HRMS, and Projects reading tabs remain available. No simulation remains active.

These exact local snapshots were verified before the source removal/homepage
replacement:

- [Former homepage](index.html.txt)
- [HRMS explorer implementation](src/main.ts.txt)
- [Scenario data and types](src/scenario-pack.ts.txt)

The `.txt` files preserve recoverable historical source, not executable pages.
Restoring this prototype would require deliberately restoring both its source
and entry point. Earlier instructions to retain page 1 are superseded by the
user's latest request; the authorization model itself is unchanged.

Commit/push gate remains frozen.
